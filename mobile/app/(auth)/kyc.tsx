import { ScreenStub } from '@/components/ui/ScreenStub';

export default function Kyc() {
  return (
    <ScreenStub
      title="Identity verification"
      subtitle="KYC"
      description="Upload ID + selfie. Verification typically completes in under 5 minutes via Sumsub. KYC tier raises your daily limits."
    />
  );
}
